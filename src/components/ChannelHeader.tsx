import React from 'react'

const ChannelHeader = () => {
  return (
    <div>ChannelHeader</div>
  )
}

// Components:
// ChannelName
// ChannelDescription
// ChannelButtons (container for channel-specific buttons)
// VoiceChannelStatus (only for voice channels)

export default ChannelHeader